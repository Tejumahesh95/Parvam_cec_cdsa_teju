#include<stdio.h>
int main(){
    int row, column;
    printf("Enter the number of rows & columns:");
    scanf("%d %d", &row, &column);
    int row, column;
    for(int j=0; j < column; j++){
        printf("*\t");
    }
    printf("*\n");
}
